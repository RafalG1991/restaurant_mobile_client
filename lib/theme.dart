import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF121212);   // ciemne tło
  static const surface = Color(0xFF222222);      // jak kafelki stolików
  static const primary = Color(0xFF28A745);      // zielony FREE
  static const accentBlue = Color(0xFF007BFF);   // niebieski (np. Show order)
  static const accentRed = Color(0xFFB5081B);    // czerwony BUSY
  static const textPrimary = Colors.white;
  static const textMuted = Color(0xFFB0B0B0);
}
